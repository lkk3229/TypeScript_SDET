// TS is a statically typed language, which means that the type of a variable is determined at compile time. However, TypeScript also supports dynamic typing, which allows you to change the type of a variable at runtime.



let data = 25;  //Number

//data = "twenty five"; // Compile time error: Type 'string' is not assignable to type 'number'.

console.log(data); // Output: 25


let num1:String="5";
let num2:number=10;

//let result = num1+num2; // The + operator performs string concatenation when one of the operands is a string.

console.log(result); // Output: "510" (string concatenation)


